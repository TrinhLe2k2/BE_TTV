@model TTV.Models.Follow

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout_User.cshtml";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Follow</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.follow_created_at)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.follow_created_at)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Book.book_title)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Book.book_title)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.User.username)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.User.username)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
